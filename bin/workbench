#!/usr/bin/env bash
set -euo pipefail

SOURCE="${BASH_SOURCE[0]}"
if [[ -L "$SOURCE" ]]; then
    TARGET="$(readlink "$SOURCE")"
    [[ "$TARGET" == /* ]] && SOURCE="$TARGET" || SOURCE="$(dirname "$SOURCE")/$TARGET"
fi
ROOT="$(cd "$(dirname "$SOURCE")/.." && pwd)"
exec python3 "$ROOT/scripts/workbench.py" "$@"
