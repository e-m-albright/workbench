name: Secret Scan

# This repo is public. Two layers, both detection-only (a finding fails the
# run; nothing is ever rewritten): on every PR/push gitleaks scans the new
# commits so a secret is caught before it lands, and the weekly scheduled run
# (fetch-depth 0) re-scans the ENTIRE history in case something slipped in
# earlier. Mirrors dotfiles' gitleaks.yml so the public repos share one gate.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 9 * * 1" # weekly, Monday morning UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0 # full history — the whole point of this scan

      - uses: gitleaks/gitleaks-action@e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e # v3.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
