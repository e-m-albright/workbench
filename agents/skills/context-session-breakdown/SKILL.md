---
name: context-session-breakdown
description: Deprecated compatibility entry. Use only for an old explicit session-breakdown command; choose handoff or reflect for current work.
disable-model-invocation: true
---

# Context Session Breakdown

This workflow has been split by intent:

- Use the `handoff` skill to create or consume temporary cross-session state.
- Use the `reflect` skill to assess what worked, what did not, and what should improve.

Do not create an Apple Notes or Desktop artifact through this compatibility entry. Ask which current workflow the user intends when the request is ambiguous.
